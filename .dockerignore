#--------------------------------------------------------------------------------
# General exclusions
#--------------------------------------------------------------------------------
# Ignore all version control and project-specific metadata
**/.git
**/.svn
**/.hg
**/.idea
**/.vscode

# Ignore IDE and editor-specific files
**/*.sublime-workspace
**/*.sublime-project
**/.DS_Store
**/Thumbs.db

# Ignore generated/temporary files
**/core
**/*.log
**/*.bak
**/*.swp
**/*~

#--------------------------------------------------------------------------------
# CMake-specific exclusions
#--------------------------------------------------------------------------------
# The build directory generated by CMake
# Assumes you are using an out-of-source build, which is best practice.
# If you name your build directory something else (e.g., `_build`),
# update this line.
build/

# CMake cache files
**/CMakeCache.txt
**/CMakeFiles/
**/install_manifest.txt

# Package files generated by CPack
**/*.deb
**/*.rpm
**/*.zip
**/*.tar.gz

# Binaries and libraries (in case they are not in the main build folder)
# Use with caution, as you might need to copy specific binaries in the Dockerfile.
**/bin/
**/lib/

#--------------------------------------------------------------------------------
# Docker-specific metadata
#--------------------------------------------------------------------------------
# Don't send the Dockerfile itself, though this is usually unnecessary since
# Docker automatically handles it.
Dockerfile
.dockerignore
